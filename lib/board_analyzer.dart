import 'package:set_solver/card.dart';
import 'package:tuple/tuple.dart';

class BoardAnalyzer {
  /// finds a Set within a list of cards and returns a set of 3 cards
  static Tuple3<Card, Card, Card>? findSet(List<Card> board) {
    return null;
  }

  /// set simulation game
  // List<Card> newCards(){}
  //
  // List<Card> generateDeck(){}
}